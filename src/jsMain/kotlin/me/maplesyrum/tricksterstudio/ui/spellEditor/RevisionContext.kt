package me.maplesyrum.tricksterstudio.ui.spellEditor

import me.maplesyrum.tricksterstudio.spell.fragment.SpellPart
import me.maplesyrum.tricksterstudio.spell.fragment.Pattern

class RevisionContext {
    fun updateSpell(rootSpellPart: SpellPart) {
        // Implement as needed
    }

    fun updateSpellWithSpell(drawingPart: SpellPart?, arg1: SpellPart) {
        // Implement as needed
    }

    fun getMacros(): Map<Pattern, SpellPart> {
        return emptyMap()
    }
}
